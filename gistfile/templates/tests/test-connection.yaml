apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "gistfile.fullname" . }}-test-connection"
  labels:
    {{- include "gistfile.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "gistfile.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
